import { CheckCircle2, CircleDot, Plus } from "lucide-react";
import { useAddPlanItem, useOrder, useWorkPlan } from "@/api/hooks";
import { Button, Card } from "@/components/ui";
import { useState } from "react";
export function ActivityPage() { const order = useOrder(); const plan = useWorkPlan(); const add = useAddPlanItem(); const [title, setTitle] = useState(""); const [amount, setAmount] = useState(100); if (!order.data || !plan.data) return <main className="page-wrap"><div className="h-80 animate-pulse rounded-3xl bg-ink-100" /></main>; return <main className="page-wrap max-w-5xl"><h1 className="display-title">Your activity</h1><p className="mt-1 text-sm text-ink-500">Things you asked for, offer, and help with.</p><section className="mt-6 grid gap-5 lg:grid-cols-2"><Card className="p-5"><p className="eyebrow">You asked for this</p><h2 className="mt-2 font-display text-xl font-extrabold">{order.data.title}</h2><p className="mt-2 text-sm text-ink-500">Maya is making the first drawing.</p><div className="mt-4 rounded-xl bg-forest-50 p-3 text-sm font-bold text-forest-800">Next: {order.data.nextAction}</div></Card><Card className="p-5"><p className="eyebrow">You offer help</p><h2 className="mt-2 font-display text-xl font-extrabold">Your invitation listing</h2><p className="mt-2 text-sm text-ink-500">Visible in Browse · Can start today</p><Button className="mt-4" variant="outline">Manage listing</Button></Card></section><section className="mt-8"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="eyebrow">Plan a bigger job</p><h2 className="font-display text-2xl font-extrabold">{plan.data.title}</h2><p className="text-sm text-ink-500">Each task has its own person and agreement.</p></div></div><div className="mt-4 grid gap-3">{plan.data.items.map((item) => <Card key={item.id} className="flex items-center gap-4 p-4"><span className="grid h-10 w-10 place-items-center rounded-xl bg-mango-50 text-mango-700">{item.state === "completed" ? <CheckCircle2 /> : <CircleDot />}</span><div className="min-w-0 flex-1"><strong>{item.title}</strong><p className="text-xs text-ink-500">{item.provider ? `${item.provider.name} · ${item.state.replaceAll("_", " ")}` : "Find someone"}</p>{item.dependency && <p className="mt-1 text-[11px] text-ink-400">{item.dependency}</p>}</div><strong className="text-forest-700">₱{item.amount}</strong></Card>)}</div><Card className="mt-4 p-4"><p className="text-sm font-bold">Add another task</p><div className="mt-3 flex flex-col gap-2 sm:flex-row"><input className="field-input" value={title} onChange={(e) => setTitle(e.target.value)} placeholder="e.g. Pick up ice" /><input className="field-input sm:w-32" type="number" value={amount} onChange={(e) => setAmount(Number(e.target.value))} /><Button disabled={!title.trim()} loading={add.isPending} leftIcon={<Plus />} onClick={() => { add.mutate({ title, amount }); setTitle(""); }}>Add task</Button></div></Card></section></main>; }
export function MePage() { return <main className="page-wrap max-w-3xl"><h1 className="display-title">Me</h1><Card className="mt-6 p-6"><h2 className="font-display text-xl font-extrabold">Rosa Mendoza</h2><p className="mt-2 text-sm text-ink-500">You can ask for help, offer services, and help Lola Nena with permission.</p><div className="mt-5 grid gap-3 sm:grid-cols-3"><div className="surface-muted p-4"><strong>Requests</strong><p className="text-xs text-ink-500">Post what you need</p></div><div className="surface-muted p-4"><strong>Listings</strong><p className="text-xs text-ink-500">Offer your work</p></div><div className="surface-muted p-4"><strong>Agent help</strong><p className="text-xs text-ink-500">Lola Nena</p></div></div></Card></main>; }
